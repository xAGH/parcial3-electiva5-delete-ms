# Configuración general
spring.application.name=delete
server.port=${SERVER_PORT:8084}

# Configuración de base de datos
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mariadb://localhost:3306/taskdb}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:taskdeleteuser}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:taskdeletepassword}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:none}

# Configuración de JPA
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
spring.jpa.show-sql=true
